# This image builds only the package wheel. An allowlist keeps local virtual
# environments, tests, coverage output, and secrets out of the build context.
**
!pyproject.toml
!uv.lock
!README.md
!src/
!src/**
src/**/__pycache__/
src/**/*.pyc
src/**/*.pyo
src/**/*.egg-info/
